# SQL 옵티마이저

# 31

- 실행계획에 SORT UNIQUE 등장
    - 1쪽 집합과 M쪽 집합의 중복 제거? 그런 식의 내용이었는데.
        - 메인쿼리 결과집합이 서브쿼리 결과집합으로 확장될 수 있어서다.
            - 이 것의 의미가 정확히?
- 서브쿼리에 qb_name(subq) 힌트가 있다.
- 또한 **`서브쿼리를 먼저 실행했다.`**
    - 서브쿼리를 필터 방식으로 처리하면 항상 메인쿼리가 드라이빙집합임
    - 그런데 서브쿼리를 먼저 실행했다 = unnest 방식으로 풀었다는 것
- 따라서 unnest 힌트는 필수 추가
- 또한 subq를 먼저 실행하기 위해  leading(거래@subq), nl조인을 위한 use_nl(c)

```sql
/*+ unnest leading(거래@subq) use_nl(c) */
```

# 32

- 억까다.
- 문제의 조건에 속하는 고객의 수가 매우 많을 것이라 생각하고 full 스캔을 한다는 결론을 유추해내는 것이 가능한가? 아니라고 본다.

# 33

- 주문일시 → 상품번호.
- 상품으로부터 주문으로 1000번의 액세스 및 결과는 60000개
- 이후 필터링된 개수가 3000개
- **`서브쿼리 필터링을 먼저 수행해서 필터링 비율을 낮춘다.`**
    - 아니 문제만 보고 이런 생각 어케하냐..

# 34

- 흠..

# 35

-
