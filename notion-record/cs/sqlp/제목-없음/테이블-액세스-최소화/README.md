# 테이블 액세스 최소화

목차: 인덱스 튜닝

# 모자른거 정리

- **Index Skip Scan**
    - **인덱스 선두 컬럼의 distinct value개수가 적고 후행 컬럼의 distinct value 개수가 많을 경우**
    - (distinct value가 적다는것은 '남', '여'처럼 보기가 별로 없다는 것)
- 인덱스 선두 컬럼이 없을 때만 index skip scan이 작동하는 것은 아니다.
- *ex1) 선두컬럼에 대한 조건절(업종유형코드)은 있지만 중간 컬럼(업종코드)에 대한 조건절이 없는 경우*

```sql
select/*+ INDEX_SS(A index1_pk) */
        기준일자, 업종코드, 체결건수, 체결수량, 거래대금
from 일별업종거래 A
where 업종유형코드='01'
and 기준일자 between '20080501' and '20080531'
```

- index1_pk : [업종유형코드 + 업종코드 + 기준일자]
- **index range scan**의 경우 업종유형코드='01'인 인덱스 구간을 모두 스캔
- **index skip scan**을 사용한다면 업종유형코드='01'인 구간에서 기준일자가 해당 범위에 있는 **레코드를 포함할 가능성이 있는 리프 블록만 골라서 액세스**

# 33

- 인덱스를 재생성(Rebuild)하면 클러스터링 팩터가 좋아진다. (x)
    - 인덱스 컬럼 순으로 정렬되도록 **테이블을 재생성**해야 팩터가 좋아짐

# 35

- 온라인 트랜잭션 vs 배치 트랜잭션
    - 온라인에서는 부분범위 처리 + 인덱스를 활용한 sort 생략
        - NL 조인 및 인덱스 활용 높음
    - 배치
        - 전체 범위 처리를 빠르게 처리하는 것을 목표로 → 해쉬 + 풀 스캔
        - 전체를 읽으면서 처리하는 것이 핵심.

# 36 (답은 맞음, 해설 궁금)

- 해설 1번이 최적인 이유

# 37

- 인덱스를 재생성하면 인덱스 스캔 비효율을 줄이는데 도움은 되나 테이블 액세스를 줄이는 데에는 도움이 되지 않음

# 38

- 인덱스 뒤쪽에 컬럼을 추가하면 클러스터링 팩터는 나빠진다. 새로운 정렬 순서를 같기 때문에.
    - 기존의 정렬 상태가 이미 높은 일관성을 가진 경우, 뒤쪽에 컬럼을 추가해도 클러스터링 팩터에 큰 영향을 주지 않을 수 있다.
    - 추가된 컬럼이 데이터의 정렬 순서에 변화를 주는 경우 클러스터링 팩터 변화 o
- **`그러니까 이건.. 좋을 수도 나쁠 수도 있다는 건가?`**

# 39

- Batch I/O
    - 테이블 블록에 대한 디스크 I/O Call을 미뤘다가 읽은 블록이 일정량 이상 쌓이면 한꺼번에 처리
    - 인덱스를 이용해서 출력하는 데이터 정렬 순서가 매번 다를 수 있다.
    - 이 기능이 작동하더라도 **`실행계획에 ‘SORT ORDER BY’가 나타나지 않으면` 부분범위 처리 가능**

# 43

- 인덱스 구성 : 할인구분코드 + 상품코드 + 업체코드 + 상품유형코드

```bash
SELECT 주문일자, 상태코드, 정상가, 할인가, 할인구분코드
FROM 상품공급
WEHRE 상품유형코드 = 'A'
AND 업체코드 = 'Z956'
AND 상품코드 = 'A0113509056'
AND 할인구분코드 BETWEEN 'A' and 'C'
ORDER BY 주문일자 DESC
```

- Index Fast Full Scan
    - 인덱스 이외의 SQL을 요구하기 때문에 불가
- Index Skip Scan
    - 할인구분코드의 NDV가 적고, 상품코드의 NDV가 많다?
        - **아니 상품코드의 NDV가 많다는 것을 문제를 보고 어떻게 유추할 수 있는가.**
- In-List iterator 변환 ok
- 조건절 나열 순서 변환은 의미없음

# 44

- index skip scan
    - ‘A’로 시작하는 컬럼의 양이 2%
- 해설 4번 이해 부족

# 46 (이해 못함)

# 48

- 같은 테이블을 같은 형식으로 2번 처리한다.
- 따라서 투입인출코드를 기점으로 CASE문으로 나눠 4개의 컬럼으로 구성
- 인덱스 추가 : 투입인출코드 + 컬럼들.

# 50

인덱스 = 주문일자 + 상품코드

**선두컬럼인 주문일자가 부등호 조건이므로 상품코드 조건절은 필터일 수밖에 없다.**

use_concat : 조건절에 or이나 inlist가 있을 때, 쿼리가 확장된 후 concatenation으로 유도 = or_expansion

no_expand : or_expansion 방지

# 51

WHERE 고객ID LIKE :CUST_ID || '%'

고객 ID가 인덱스 선두 컬럼이면 무조건 range scan은 가능. 그러나 **null인 경**우 인덱스에서 모든 데이터를 스캔해야 하는 비효율이 있다.

# 52

옵션조건에 NVL, DECODE 사용 UNION ALL 형태로 작동 가능하므로 효율적

옵션조건에 CASE 문을 사용하면 UNION ALL 분기가 불가

# 61

부분범위 처리 가능하다 = SORT 연산 생략 가능하다.

1. '=' 연산자가 들어간 조건절 컬럼 설정

2. ORDER BY에 기술된 컬럼 추가

3. '=' 연산자가 아닌 컬럼은 데이터 분포도에 따라 추가 여부 결정

# 64

**인덱스 선두 컬럼에 NULL이 들어가면** LIKE에서 인덱스 전체 스캔 (사용자가 값을 입력하지 않을 때)

NULL 허용 컬럼에 LIKE 연산자를 사용하면 결과집합에 오류가 생길 수 있다.

# 65

인덱스 선행 컬럼이 '=' 조건인 상태에서 **첫 번째 나타나는 범위검색(LIKE, BETWEEN, 부등호 등) 조건까지**가 액세스 조건이다.

OR 조건은 인덱스 액세스 조건으로 사용 불가

# 66

가)에 대하여

옵티마이저가 계약_X1을 선택한 상황에서 주민등록번호만 입력한다 -> 비효율

옵티마이저가 계약_X2을 선택한 상황에서 계약자명만 입력한다 -> 비효율

**Q : 적절한 인덱스를 사용하겠다는 것을 옵티마이저는 언제 판단하는 건가?**
