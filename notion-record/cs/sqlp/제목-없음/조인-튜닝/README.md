# 조인 튜닝

목차: 인덱스튜닝

## 1

ordered 힌트 : from 절에 나열된 순서대로 조인

ordered use_nl(c) use_hash(t) = leading(o c t) use_nl(c) use_hash(t)

## 6

소량 조인의 경우에도 NL 조인이 유리하다.

## 8

69개의 Row를 읽기 위해 922개의 블록을 스캔했다.

따라서 컬럼의 위치를 조정하는 것으로 해결 가능

## 10

거래쪽으로 35272번 JOIN (ROW 수 체크)하는 과정에서 105818개의 block을 읽고,

TABLE ACCESS까지 총 156123개의 블록을 읽음.

**조인 과정에 발생하는 I/O가 전체 I/O의 91%를 차지한다.**

## 11

NL조인을 할 때 STRD_GRP_ID와 STRD_ID 인덱스가 필요하지만 **PRA_HST_STC의 입장에서는 필요없다.**

조인을 위한 컬럼이기 때문에 그러한 듯?

인덱스에 ORDER BY 컬럼을 추가해서 성능 효과를 얻으려면 부분범위 처리가 가능한 상황이라는 전제가 필요

## 23

swap_join_inputs가 우선되는 힌트다.

## 24

## 26

## 27

결과를 보고..

## 28

use_hash(b)를 use_nl(b)로 수정했을 때의 문제점이 무엇인가.

## 32

실행계획 상 위에 있는 것에 swap_join_inputs

## 40

3번에 대하여

## 45
